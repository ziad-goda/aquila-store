import { jsx, jsxs } from "react/jsx-runtime";
import { useNavigate, Link } from "@tanstack/react-router";
import { useState, useEffect } from "react";
import { s as supabase } from "./client-CskvuBrI.js";
import { toast } from "sonner";
import { l as logo } from "./logo-Kw5cEugR.js";
import { R as Route } from "./router-D_0TxxYN.js";
import "@supabase/supabase-js";
import "@tanstack/react-query";
import "zod";
function AuthPage() {
  const {
    redirect
  } = Route.useSearch();
  const navigate = useNavigate();
  const [mode, setMode] = useState("login");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [name, setName] = useState("");
  const [loading, setLoading] = useState(false);
  useEffect(() => {
    supabase.auth.getSession().then(({
      data: {
        session
      }
    }) => {
      if (session) navigate({
        to: redirect ?? "/account"
      });
    });
  }, [redirect, navigate]);
  const submit = async (e) => {
    e.preventDefault();
    setLoading(true);
    try {
      if (mode === "signup") {
        const {
          error
        } = await supabase.auth.signUp({
          email,
          password,
          options: {
            emailRedirectTo: `${window.location.origin}/account`,
            data: {
              full_name: name
            }
          }
        });
        if (error) throw error;
        toast.success("تم إنشاء حسابك ✓");
        navigate({
          to: redirect ?? "/account"
        });
      } else {
        const {
          error
        } = await supabase.auth.signInWithPassword({
          email,
          password
        });
        if (error) throw error;
        toast.success("مرحباً بعودتك ✓");
        navigate({
          to: redirect ?? "/account"
        });
      }
    } catch (err) {
      const msg = err instanceof Error ? err.message : "حدث خطأ";
      toast.error(msg.includes("Invalid") ? "بيانات غير صحيحة" : msg.includes("already") ? "البريد مسجّل بالفعل" : msg);
    } finally {
      setLoading(false);
    }
  };
  return /* @__PURE__ */ jsx("main", { className: "min-h-screen flex items-center justify-center px-6 bg-background", children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-md", children: [
    /* @__PURE__ */ jsxs(Link, { to: "/", className: "flex flex-col items-center mb-10", children: [
      /* @__PURE__ */ jsx("img", { src: logo, alt: "Aquila", width: 64, height: 64, className: "h-16 w-16 object-contain" }),
      /* @__PURE__ */ jsx("span", { className: "font-display text-3xl tracking-widest gradient-gold-text mt-2", children: "AQUILA" })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "p-8 border border-border/40 bg-card/60 rounded-sm", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex border-b border-border/40 mb-6", children: [
        /* @__PURE__ */ jsx("button", { onClick: () => setMode("login"), className: `flex-1 pb-3 text-sm tracking-widest uppercase ${mode === "login" ? "text-gold border-b-2 border-gold" : "text-muted-foreground"}`, children: "دخول" }),
        /* @__PURE__ */ jsx("button", { onClick: () => setMode("signup"), className: `flex-1 pb-3 text-sm tracking-widest uppercase ${mode === "signup" ? "text-gold border-b-2 border-gold" : "text-muted-foreground"}`, children: "تسجيل" })
      ] }),
      /* @__PURE__ */ jsxs("form", { onSubmit: submit, className: "space-y-4", children: [
        mode === "signup" && /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("label", { className: "block text-xs tracking-widest uppercase text-gold mb-2", children: "الاسم" }),
          /* @__PURE__ */ jsx("input", { required: true, value: name, onChange: (e) => setName(e.target.value), className: "w-full bg-input border border-border px-4 py-3 text-sm focus:border-gold focus:outline-none rounded-sm" })
        ] }),
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("label", { className: "block text-xs tracking-widest uppercase text-gold mb-2", children: "البريد الإلكتروني" }),
          /* @__PURE__ */ jsx("input", { type: "email", required: true, value: email, onChange: (e) => setEmail(e.target.value), className: "w-full bg-input border border-border px-4 py-3 text-sm focus:border-gold focus:outline-none rounded-sm", dir: "ltr" })
        ] }),
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("label", { className: "block text-xs tracking-widest uppercase text-gold mb-2", children: "كلمة المرور" }),
          /* @__PURE__ */ jsx("input", { type: "password", required: true, minLength: 6, value: password, onChange: (e) => setPassword(e.target.value), className: "w-full bg-input border border-border px-4 py-3 text-sm focus:border-gold focus:outline-none rounded-sm" })
        ] }),
        /* @__PURE__ */ jsx("button", { type: "submit", disabled: loading, className: "w-full bg-gold text-gold-foreground py-3.5 text-sm tracking-widest uppercase font-semibold shadow-gold hover:opacity-90 transition-opacity disabled:opacity-50", children: loading ? "جاري..." : mode === "login" ? "دخول" : "إنشاء حساب" })
      ] })
    ] }),
    /* @__PURE__ */ jsx(Link, { to: "/", className: "block text-center text-sm text-muted-foreground hover:text-gold mt-6", children: "← العودة للمتجر" })
  ] }) });
}
export {
  AuthPage as component
};
