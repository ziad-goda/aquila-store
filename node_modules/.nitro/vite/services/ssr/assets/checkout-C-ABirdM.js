import { jsxs, Fragment, jsx } from "react/jsx-runtime";
import { useNavigate, Link } from "@tanstack/react-router";
import { H as Header, F as Footer } from "./footer-CnmVwV6a.js";
import { u as useCart } from "./router-D_0TxxYN.js";
import { u as useAuth } from "./use-auth-BS3YO_BT.js";
import { s as supabase } from "./client-CskvuBrI.js";
import { useState } from "react";
import { toast } from "sonner";
import "lucide-react";
import "./logo-Kw5cEugR.js";
import "@tanstack/react-query";
import "zod";
import "@supabase/supabase-js";
function CheckoutPage() {
  const {
    items,
    total,
    clearCart
  } = useCart();
  const {
    user,
    loading
  } = useAuth();
  const navigate = useNavigate();
  const [form, setForm] = useState({
    name: "",
    phone: "",
    address: "",
    city: "",
    notes: ""
  });
  const [coupon, setCoupon] = useState("");
  const [discount, setDiscount] = useState(0);
  const [submitting, setSubmitting] = useState(false);
  const shipping = total > 500 ? 0 : 30;
  const discountAmount = total * discount / 100;
  const finalTotal = total + shipping - discountAmount;
  const applyCoupon = async () => {
    if (!coupon) return;
    const {
      data
    } = await supabase.from("coupons").select("*").eq("code", coupon.toUpperCase()).eq("active", true).maybeSingle();
    if (!data) return toast.error("الكود غير صالح");
    setDiscount(data.discount_percent);
    toast.success(`تم تطبيق خصم ${data.discount_percent}% ✓`);
  };
  const submit = async (e) => {
    e.preventDefault();
    if (!user) return navigate({
      to: "/auth",
      search: {
        redirect: "/checkout"
      }
    });
    if (items.length === 0) return toast.error("السلة فارغة");
    setSubmitting(true);
    try {
      const {
        data: order,
        error
      } = await supabase.from("orders").insert({
        user_id: user.id,
        subtotal: total,
        discount: discountAmount,
        shipping,
        total: finalTotal,
        coupon_code: discount > 0 ? coupon.toUpperCase() : null,
        shipping_name: form.name,
        shipping_phone: form.phone,
        shipping_address: form.address,
        shipping_city: form.city,
        notes: form.notes || null
      }).select().single();
      if (error || !order) throw error;
      const orderItems = items.map((it) => ({
        order_id: order.id,
        product_id: it.productId,
        product_name: it.name,
        product_image: it.image,
        size: it.size,
        color: it.color,
        quantity: it.quantity,
        unit_price: it.price
      }));
      const {
        error: itemsError
      } = await supabase.from("order_items").insert(orderItems);
      if (itemsError) throw itemsError;
      clearCart();
      toast.success(`تم استلام طلبك ${order.order_number} ✓`);
      navigate({
        to: "/account"
      });
    } catch (err) {
      console.error(err);
      toast.error("حدث خطأ، حاول مجدداً");
    } finally {
      setSubmitting(false);
    }
  };
  if (loading) return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(Header, {}),
    /* @__PURE__ */ jsx("div", { className: "container-aquila py-32 text-center", children: "..." }),
    /* @__PURE__ */ jsx(Footer, {})
  ] });
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(Header, {}),
    /* @__PURE__ */ jsxs("main", { className: "container-aquila py-12", children: [
      /* @__PURE__ */ jsx("h1", { className: "text-4xl font-display mb-8", children: "إتمام الطلب" }),
      !user && /* @__PURE__ */ jsxs("div", { className: "mb-6 p-4 border border-gold/30 bg-gold/5 rounded-sm text-sm", children: [
        "يجب ",
        /* @__PURE__ */ jsx(Link, { to: "/auth", search: {
          redirect: "/checkout"
        }, className: "text-gold underline", children: "تسجيل الدخول" }),
        " لإتمام الطلب"
      ] }),
      /* @__PURE__ */ jsxs("form", { onSubmit: submit, className: "grid lg:grid-cols-3 gap-10", children: [
        /* @__PURE__ */ jsxs("div", { className: "lg:col-span-2 space-y-4", children: [
          /* @__PURE__ */ jsx("h2", { className: "text-xl font-display border-b border-border/40 pb-3", children: "معلومات الشحن" }),
          /* @__PURE__ */ jsx(Input, { label: "الاسم الكامل", value: form.name, onChange: (v) => setForm({
            ...form,
            name: v
          }), required: true }),
          /* @__PURE__ */ jsx(Input, { label: "رقم الجوال", value: form.phone, onChange: (v) => setForm({
            ...form,
            phone: v
          }), required: true }),
          /* @__PURE__ */ jsx(Input, { label: "العنوان", value: form.address, onChange: (v) => setForm({
            ...form,
            address: v
          }), required: true }),
          /* @__PURE__ */ jsx(Input, { label: "المدينة", value: form.city, onChange: (v) => setForm({
            ...form,
            city: v
          }), required: true }),
          /* @__PURE__ */ jsxs("div", { children: [
            /* @__PURE__ */ jsx("label", { className: "block text-xs tracking-widest uppercase text-gold mb-2", children: "ملاحظات (اختياري)" }),
            /* @__PURE__ */ jsx("textarea", { value: form.notes, onChange: (e) => setForm({
              ...form,
              notes: e.target.value
            }), rows: 3, className: "w-full bg-input border border-border px-4 py-2.5 text-sm focus:border-gold focus:outline-none rounded-sm" })
          ] })
        ] }),
        /* @__PURE__ */ jsxs("aside", { className: "lg:sticky lg:top-24 lg:self-start p-6 border border-border/40 bg-card/50 rounded-sm space-y-4 h-fit", children: [
          /* @__PURE__ */ jsx("h2", { className: "text-xl font-display border-b border-border/40 pb-3", children: "ملخص الطلب" }),
          /* @__PURE__ */ jsx("div", { className: "space-y-2 max-h-60 overflow-auto", children: items.map((it) => /* @__PURE__ */ jsxs("div", { className: "flex justify-between text-sm", children: [
            /* @__PURE__ */ jsxs("span", { className: "text-muted-foreground", children: [
              it.name,
              " × ",
              it.quantity
            ] }),
            /* @__PURE__ */ jsxs("span", { children: [
              (it.price * it.quantity).toFixed(0),
              " ر.س"
            ] })
          ] }, `${it.productId}-${it.size}-${it.color}`)) }),
          /* @__PURE__ */ jsxs("div", { className: "border-t border-border/40 pt-3 space-y-2", children: [
            /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
              /* @__PURE__ */ jsx("input", { value: coupon, onChange: (e) => setCoupon(e.target.value), placeholder: "كود خصم", className: "flex-1 bg-input border border-border px-3 py-2 text-sm rounded-sm focus:border-gold focus:outline-none" }),
              /* @__PURE__ */ jsx("button", { type: "button", onClick: applyCoupon, className: "px-4 py-2 border border-gold text-gold text-sm rounded-sm hover:bg-gold hover:text-gold-foreground transition-colors", children: "تطبيق" })
            ] }),
            /* @__PURE__ */ jsxs("div", { className: "flex justify-between text-sm", children: [
              /* @__PURE__ */ jsx("span", { children: "المجموع" }),
              /* @__PURE__ */ jsxs("span", { children: [
                total.toFixed(2),
                " ر.س"
              ] })
            ] }),
            discount > 0 && /* @__PURE__ */ jsxs("div", { className: "flex justify-between text-sm text-gold", children: [
              /* @__PURE__ */ jsxs("span", { children: [
                "الخصم (",
                discount,
                "%)"
              ] }),
              /* @__PURE__ */ jsxs("span", { children: [
                "-",
                discountAmount.toFixed(2),
                " ر.س"
              ] })
            ] }),
            /* @__PURE__ */ jsxs("div", { className: "flex justify-between text-sm", children: [
              /* @__PURE__ */ jsx("span", { children: "الشحن" }),
              /* @__PURE__ */ jsx("span", { children: shipping === 0 ? "مجاني" : `${shipping} ر.س` })
            ] }),
            /* @__PURE__ */ jsxs("div", { className: "flex justify-between text-lg font-semibold border-t border-border/40 pt-2", children: [
              /* @__PURE__ */ jsx("span", { children: "الإجمالي" }),
              /* @__PURE__ */ jsxs("span", { className: "text-gold", children: [
                finalTotal.toFixed(2),
                " ر.س"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ jsx("button", { type: "submit", disabled: submitting || items.length === 0, className: "w-full bg-gold text-gold-foreground py-3.5 text-sm tracking-widest uppercase font-semibold shadow-gold hover:opacity-90 transition-opacity disabled:opacity-50", children: submitting ? "جاري التأكيد..." : "تأكيد الطلب" })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx(Footer, {})
  ] });
}
function Input({
  label,
  value,
  onChange,
  required,
  type = "text"
}) {
  return /* @__PURE__ */ jsxs("div", { children: [
    /* @__PURE__ */ jsx("label", { className: "block text-xs tracking-widest uppercase text-gold mb-2", children: label }),
    /* @__PURE__ */ jsx("input", { type, required, value, onChange: (e) => onChange(e.target.value), className: "w-full bg-input border border-border px-4 py-2.5 text-sm focus:border-gold focus:outline-none rounded-sm" })
  ] });
}
export {
  CheckoutPage as component
};
