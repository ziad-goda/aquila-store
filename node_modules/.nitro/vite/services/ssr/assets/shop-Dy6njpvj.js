import { jsxs, Fragment, jsx } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { s as supabase } from "./client-CskvuBrI.js";
import { H as Header, F as Footer } from "./footer-CnmVwV6a.js";
import { P as ProductCard } from "./product-card-Chu-68uQ.js";
import { useState, useMemo } from "react";
import { Search } from "lucide-react";
import { a as Route } from "./router-D_0TxxYN.js";
import "@supabase/supabase-js";
import "@tanstack/react-router";
import "./logo-Kw5cEugR.js";
import "./image-C8nXBy0i.js";
import "sonner";
import "zod";
function ShopPage() {
  const {
    category: catParam
  } = Route.useSearch();
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState(catParam ?? "all");
  const [size, setSize] = useState("all");
  const [color, setColor] = useState("all");
  const [sort, setSort] = useState("new");
  const [maxPrice, setMaxPrice] = useState(1500);
  const {
    data: products = []
  } = useQuery({
    queryKey: ["products", "all"],
    queryFn: async () => {
      const {
        data
      } = await supabase.from("products").select("*");
      return data ?? [];
    }
  });
  const filtered = useMemo(() => {
    let arr = [...products];
    if (category !== "all") arr = arr.filter((p) => p.category === category);
    if (search) arr = arr.filter((p) => p.name.includes(search));
    if (size !== "all") arr = arr.filter((p) => p.sizes.includes(size));
    if (color !== "all") arr = arr.filter((p) => p.colors.includes(color));
    arr = arr.filter((p) => p.price <= maxPrice);
    if (sort === "price-asc") arr.sort((a, b) => a.price - b.price);
    else if (sort === "price-desc") arr.sort((a, b) => b.price - a.price);
    else if (sort === "bestseller") arr.sort((a, b) => Number(b.is_bestseller) - Number(a.is_bestseller));
    else arr.sort((a, b) => +new Date(b.created_at) - +new Date(a.created_at));
    return arr;
  }, [products, category, search, size, color, sort, maxPrice]);
  const allColors = useMemo(() => Array.from(new Set(products.flatMap((p) => p.colors))), [products]);
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(Header, {}),
    /* @__PURE__ */ jsxs("main", { className: "container-aquila py-12", children: [
      /* @__PURE__ */ jsxs("div", { className: "text-center mb-12", children: [
        /* @__PURE__ */ jsx("span", { className: "text-xs tracking-[0.3em] uppercase text-gold", children: "المتجر" }),
        /* @__PURE__ */ jsx("h1", { className: "text-5xl font-display mt-2", children: "مجموعتنا" })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "flex flex-col lg:flex-row gap-8", children: [
        /* @__PURE__ */ jsxs("aside", { className: "lg:w-64 space-y-6 lg:sticky lg:top-24 lg:self-start", children: [
          /* @__PURE__ */ jsxs("div", { className: "relative", children: [
            /* @__PURE__ */ jsx(Search, { className: "absolute right-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" }),
            /* @__PURE__ */ jsx("input", { value: search, onChange: (e) => setSearch(e.target.value), placeholder: "بحث...", className: "w-full bg-input border border-border pr-10 pl-3 py-2.5 text-sm focus:border-gold focus:outline-none rounded-sm" })
          ] }),
          /* @__PURE__ */ jsx(FilterGroup, { title: "الفئة", children: [{
            v: "all",
            l: "الكل"
          }, {
            v: "hoodies",
            l: "هوديز"
          }, {
            v: "pants",
            l: "بناطيل"
          }].map((o) => /* @__PURE__ */ jsx("button", { onClick: () => setCategory(o.v), className: `block w-full text-right py-1.5 text-sm transition-colors ${category === o.v ? "text-gold" : "text-muted-foreground hover:text-foreground"}`, children: o.l }, o.v)) }),
          /* @__PURE__ */ jsx(FilterGroup, { title: "المقاس", children: /* @__PURE__ */ jsx("div", { className: "flex flex-wrap gap-2", children: ["all", "S", "M", "L", "XL", "XXL"].map((s) => /* @__PURE__ */ jsx("button", { onClick: () => setSize(s), className: `min-w-10 h-9 px-3 text-xs border rounded-sm transition-colors ${size === s ? "bg-gold text-gold-foreground border-gold" : "border-border hover:border-gold"}`, children: s === "all" ? "الكل" : s }, s)) }) }),
          /* @__PURE__ */ jsx(FilterGroup, { title: "اللون", children: /* @__PURE__ */ jsxs("select", { value: color, onChange: (e) => setColor(e.target.value), className: "w-full bg-input border border-border px-3 py-2 text-sm rounded-sm focus:border-gold focus:outline-none", children: [
            /* @__PURE__ */ jsx("option", { value: "all", children: "جميع الألوان" }),
            allColors.map((c) => /* @__PURE__ */ jsx("option", { value: c, children: c }, c))
          ] }) }),
          /* @__PURE__ */ jsx(FilterGroup, { title: `السعر: حتى ${maxPrice} ر.س`, children: /* @__PURE__ */ jsx("input", { type: "range", min: 100, max: 1500, step: 50, value: maxPrice, onChange: (e) => setMaxPrice(+e.target.value), className: "w-full accent-[var(--gold)]" }) }),
          /* @__PURE__ */ jsx(FilterGroup, { title: "ترتيب", children: /* @__PURE__ */ jsxs("select", { value: sort, onChange: (e) => setSort(e.target.value), className: "w-full bg-input border border-border px-3 py-2 text-sm rounded-sm focus:border-gold focus:outline-none", children: [
            /* @__PURE__ */ jsx("option", { value: "new", children: "الأحدث" }),
            /* @__PURE__ */ jsx("option", { value: "bestseller", children: "الأكثر مبيعاً" }),
            /* @__PURE__ */ jsx("option", { value: "price-asc", children: "السعر: من الأقل" }),
            /* @__PURE__ */ jsx("option", { value: "price-desc", children: "السعر: من الأعلى" })
          ] }) })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "flex-1", children: [
          /* @__PURE__ */ jsxs("div", { className: "text-sm text-muted-foreground mb-4", children: [
            filtered.length,
            " منتج"
          ] }),
          filtered.length === 0 ? /* @__PURE__ */ jsx("div", { className: "text-center py-20 text-muted-foreground", children: "لا توجد منتجات مطابقة" }) : /* @__PURE__ */ jsx("div", { className: "grid grid-cols-2 lg:grid-cols-3 gap-6", children: filtered.map((p) => /* @__PURE__ */ jsx(ProductCard, { product: p }, p.id)) })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx(Footer, {})
  ] });
}
function FilterGroup({
  title,
  children
}) {
  return /* @__PURE__ */ jsxs("div", { children: [
    /* @__PURE__ */ jsx("h3", { className: "text-xs tracking-widest uppercase text-gold mb-3", children: title }),
    children
  ] });
}
export {
  ShopPage as component
};
